name: CI-2

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  call-build-test:
    uses: ./.github/workflows/build_test.yml
  # call-performace-test:
  #   uses: ./.github/workflows/performance_test.yml
