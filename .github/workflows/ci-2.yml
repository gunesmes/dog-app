name: CI-2

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  call-build-test:
    uses: ./.github/workflows/dog-registration/build_test.yml
  call-performance-test:
    uses: ./.github/workflows/dog-registration/performance_test.yml
